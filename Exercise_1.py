""" When the plus sign is used, it raises an error
given that we cannot add a string and an integer.
so to achieve this output, we instead use a comma to
concatenate the evaluation and the string."""
print("2 + 2 = ", (2+2))